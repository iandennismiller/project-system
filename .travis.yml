language: python
cache: pip
python:
  - 2.7
  - 3.5
install:
  - make dev install
script:
  - nosetests -w project_system -c etc/tests.cfg --with-coverage --cover-package=project_system
after_success:
  - coveralls --rcfile=.coveragerc
